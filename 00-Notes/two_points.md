# 双指针知识点总结

## 🚀 双指针（Two Pointers）基础

双指针不是一种具体的数据结构，而是一种**解题技巧**。它通过维护两个变量（指针或下标）同步或异步地移动，来减少不必要的循环，将复杂度从  降到 。

在 ACM 中，双指针主要分为三大流派：

### 1. 左右指针（对撞指针）

* **场景**：通常用于**有序**数组或字符串。
* **动作**：一个指针在开头 `left`，一个在末尾 `right`，向中间靠拢。
* **经典题目**：
* **二分查找**（本质也是双指针）。
* **15. 三数之和**：固定一个数，剩下的两个数用左右指针找目标。
* **11. 盛最多水的容器**：每次移动较短的那根柱子。



### 2. 快慢指针（追及指针）

* **场景**：通常用于**链表**或判定**循环**。
* **动作**：`slow` 每次走一步，`fast` 每次走两步。
* **经典题目**：
* **141. 环形链表**：如果 fast 追上了 slow，说明有环。
* **876. 链表的中间结点**：当 fast 到达终点，slow 恰好在中间。



### 3. 滑动窗口（同向指针）

* **场景**：用于处理**子数组**或**子字符串**问题。
* **动作**：两个指针 `left` 和 `right` 都从起点出发，`right` 不断扩大窗口，当满足某种条件时，`left` 开始收缩。
* **经典题目**：
* **3. 无重复字符的最长子串**。



---

## 🛡️ 教练的第一个双指针挑战

既然你已经准备好了，我们不整虚的，直接上题！

**题目：167. 两数之和 II - 输入有序数组**

> 给你一个下标从 1 开始的**已按非递减顺序排列**的整数数组 `numbers` ，请你从数组中找出两个数满足相加之和等于目标数 `target` 。

**教练提示**：

1. 注意关键词：**已排序（Sorted）**。
2. 如果你用暴力两层循环，那是 。
3. 如果你用**左右指针**：
* 如果 `nums[left] + nums[right] > target`，说明和太大了，应该谁往中间移？
* 如果 `nums[left] + nums[right] < target`，说明和太小了，应该谁往中间移？



**你的初步想法是什么？试着用左右指针的逻辑描述一下。**